= TextMate grammar for https://pkl-lang.org[Pkl]

== Development

Have Pkl installed in your path.

Change the grammar or snippets in the `pkl` folder. +
Run `npm run build` to generate the syntax and snippet files.